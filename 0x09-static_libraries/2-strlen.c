#include "main.h"
#include <string.h>

/**
 * _strlen - a function that return
 * the length of a string
 *
 * @s: value of the string
 *
 * Return: to strlen
 **/

int _strlen(char *s)
{
	return (strlen(s));
}
